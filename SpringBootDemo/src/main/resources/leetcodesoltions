  if(x < 0 || (x % 10 == 0 && x != 0)) {
            return false;
        }
        int reveNumber = 0;
        while(x > reveNumber) {
            reveNumber = reveNumber * 10 + x % 10;
            x /= 10;
        }

return x == revertedNumber || x == revertedNumber/10;
    }
	*****************************************************8